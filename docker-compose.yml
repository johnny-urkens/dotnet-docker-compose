version: '3.4'

services:
  test-backend:
    image: r0790759/test-backend
#     build:
#       context: .
#       dockerfile: /Dockerfile
    ports:
      - "8080:8080"
    depends_on:
          - db

  db:
    image: "mcr.microsoft.com/mssql/server:2022-latest"
    ports:
      - "1433:1433"

    environment:
        SA_PASSWORD: "S3cur3P@ssW0rd!"
        ACCEPT_EULA: "Y"
